#cloud-config
hostname: server1

# Set password for fedora user
chpasswd:
  list: |
    fedora:fedora
  expire: False

# Configure SSH
ssh_pwauth: True

# Remove eth0 default route after boot
runcmd:
  - ip route del default via 192.168.122.1 dev eth0 || true
